(dyn-let (load "./library/mac.bl")
    (let ((user-input (read)))
        (cond (((= user-input "a") 0)
               ((= user-input "b") 1)
               ((= user-input "c") 2)
               ((= user-input "d") 3)
               (#t 4)))))
